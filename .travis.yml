language: python
python:
    - "2.7"
    - "3.6"
install:
    - pip install .
    - python -c "import seminario; print(seminario.__version__)"
script:
    - pytest
after_success:
    - pip install coveralls
    - coverage run --source=seminario
    - coveralls
